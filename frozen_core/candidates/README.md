# Candidate policy

Candidate discoveries remain in the provenance registry and scan outputs. `HISTORICAL_POST_DATA`, `UNVERIFIED`, and `MISSING` entries are not silently admitted into the frozen core.
