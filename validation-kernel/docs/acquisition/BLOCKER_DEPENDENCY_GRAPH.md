# Blocker Dependency Graph

The machine-readable counterpart is blocker_dependency_graph.json.

    complete authentic frozen numerical bundle
      -> Step 06 certification
      -> LSC prediction
      -> primary sequence T11 -> T7 -> T3 -> T1 -> T10

    collaboration covariance/likelihood
      -> COV_COLLABORATION
      -> inferential rather than sensitivity-only conclusions

    explicit LSC-to-KATRIN / LSC-to-IceCube equations
      -> authorized external mapping
      -> T10 external veto

    exact production geometry
      -> production M3 sterile baseline
      -> T10 comparison

    timing + surveyed orientation + exact geometry
      + active tensor + units + frame transforms
      -> T12

    BEST-2 as-built source/geometry/exposure inputs
      -> future prediction freeze
      -> future blind BEST-2 prediction

No edge in this graph authorizes execution during STEP 05.
