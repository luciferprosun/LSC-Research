# LSC 6.3.1 Draft Changelog

Status: **DRAFT - NO EXECUTABLE 6.3.1 OBJECT OR RELEASE EXISTS**

This skeleton records the publication boundary established in STEP 06. Numerical entries must be added only after explicit STEP 07 decisions and must never be backdated to 6.3.0.

## Preserved from 6.3.0

- historical identity and lineage as a referenced predecessor;
- canonical symbolic E1-E12 set, conditional on no STEP 07 physics change;
- authentic parameter names and roles;
- frozen/non-frozen boundary and negative evidence;
- historical provenance and adverse/negative artifacts.

## Formalized

- machine-readable E1-E12 descriptors and dependency graph;
- immutable model identity and provenance enforcement;
- parameter, unit, frame, and tensor schemas;
- fail-closed execution contracts;
- dataset, validation, statistical, and preregistration contracts;
- recoverability and successor-publication boundary.

## Newly specified

To be completed in STEP 07. No numerical values are present in this draft.

- complete parameter vector and ordering: `UNRESOLVED`;
- numerical values and uncertainties: `UNRESOLVED`;
- response functions: `UNRESOLVED`;
- units and frames: `UNRESOLVED`;
- active tensor values and orientation: `UNRESOLVED`;
- initialization, bounds, defaults, and evaluation conventions: `UNRESOLVED`.

Every resolved entry must be labelled `NEW_NUMERICAL_SPECIFICATION_6_3_1` unless direct evidence justifies another genesis-contract class.

## Reproducibility changes

- planned complete machine-readable parameter bundle;
- planned deterministic evaluator and input/output schemas;
- planned golden reference vectors;
- planned environment lock and clean-checkout reproduction;
- planned release and parameter decision manifests.

## Validation infrastructure

- formal T1-T12 registry and prerequisites;
- frozen multiple-testing policy;
- verified independent statistical and baseline engine;
- retrospective/prospective two-track policy;
- BEST-2 pre-unblinding freeze protocol.

## Known limitations

- historical 6.3.0 numerical state remains unrecoverable with current evidence;
- no executable 6.3.1 parameter bundle exists;
- collaboration covariance/likelihood gaps remain;
- KATRIN and IceCube mappings remain unjustified;
- production M3 geometry is incomplete;
- canonical hierarchical M4 remains incomplete;
- T12 directional prerequisites remain incomplete;
- BEST-2 authoritative as-built inputs remain future-only.

## Historical compatibility

Symbolic compatibility may be claimed only after equation-level equivalence tests. Numerical identity with 6.3.0 is not claimed. Historical data evaluations must remain `RETROSPECTIVE_NON_BLIND` where the model is exposed.

## Breaking changes

None declared in STEP 06. If STEP 07 changes E1-E12, physical semantics, tensor meaning, observable mapping, or model class, the candidate version must be escalated to 6.4.0 or 7.0 before release.

## Scientific interpretation boundary

6.3.1 is intended to make its own specification executable and reproducible. It does not retroactively prove that historical 6.3.0 was numerically complete, and it cannot convert previously exposed data into blind confirmation.
