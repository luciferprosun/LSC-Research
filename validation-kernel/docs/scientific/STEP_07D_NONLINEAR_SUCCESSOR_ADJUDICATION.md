# STEP 07D RESULT

Project: LSC Validation Kernel

Status: **PASS**

Adjudication verdict: **`EXACT_A2_FINITE_DILATION_SCIENTIFICALLY_ADMISSIBLE`**

Decision: **PATH N1**

Recommended successor: **LSC 6.5.0**

The exact capture-cross-section argument dilation is a mathematically coherent one-parameter group completion of the owner-authorized meaning `alpha_0 = Delta ln E`. It preserves the A2 physical interpretation, null limit, universal sharing, tensor-off profile and frozen cross-section payload. It is nevertheless a new physical closure proposed after the adverse 6.4.0 determination. It is not a repair, bound widening, successful refit, validation result, or authorized implementation.

## A. LSC 6.4.0 adverse-result preservation

PASS.

The complete STEP 07C attempt is bound to commit `e53db798f1f3f448034a6e0af6399e8256189cd5` by `LSC_6_4_0_ADVERSE_RESULT_FREEZE.json`. Seventeen scientific, numerical, implementation and result artifacts are checked both against their working-tree SHA-256 and the exact Git blob at that commit.

The preserved status is `DEVELOPMENT_PARAMETER_DETERMINATION_FAILED`:

- response SHA-256: `c548592b44d0f75ecfd24f2392e07492920711415b207cf74c4bd31526fdceb8`;
- cross-section payload SHA-256: `55dc0006cbb2bca205f03162521dbbc75ba68af223810b312113398528a49da5`;
- pre-fit freeze SHA-256: `60380837f348cc41dc3d0b240fcba0b61a95c9f61cfab2bc3f5e7db71e9ea179`;
- adverse-result payload SHA-256: `86ab235e39237ef7ef86f3ce6609c2a33b9e36aab1b3940fac6e0736b8e08f7a`;
- adverse-freeze SHA-256: `0a45c0f3e03d1691d3f9141de6c9a2cc33a0ad7ed878422f045b8b7a714275bc`.

No 6.4.0 response, bound, spline, payload, dataset, covariance, objective, result or code was changed in STEP 07D. The constrained boundary value was not promoted, no parameter bundle was created and no refit was performed.

## B. Why parameter determination failed

Implementation and preregistration passed. The scientific acceptance criterion did not.

The linear model required `alpha_0` to stay inside `[-0.06137569484389956, 0.09541395625505644]`, the region frozen before fitting in which its difference from a finite dilation was at most 1% at every supported source line. The unconstrained diagnostic optimum, `-0.1159773471000784`, lies below that interval. The bounded optimizer therefore returned `-0.06137569305004568`, on the lower boundary.

This is a `BOUNDARY_DOMINATED` development-parameter determination failure. It is neither `MODEL_FALSIFIED` nor a successful fit. The old bound remains unchanged.

## C. Exact finite-dilation derivation

### Derivation A - finite transformation first

Preserve the authorized parameter meaning as an exact logarithmic displacement:

`alpha_0 = ln(E_sigma_prime/E)`.

Therefore:

`E_sigma_prime = E exp(alpha_0)`.

The authorized object receiving that displacement is only the argument of the frozen capture cross section. Thus:

`T_alpha sigma_m(E) = sigma_m(E exp(alpha_0))`,

`K_m(E;alpha_0) = sigma_m(E exp(alpha_0)) / sigma_m(E)`,

`S_m(E;alpha_0) = K_m(E;alpha_0) - 1`.

### Derivation B - generator/exponential flow

Let:

`D = d/d ln E = E d/dE`.

The exponential flow generated by `D` is:

`exp(alpha_0 D) sigma_m(E) = sigma_m(E exp(alpha_0))`.

It satisfies:

`T_alpha T_beta = T_(alpha+beta)`

and the corresponding ratio cocycle:

`K(E;alpha+beta) = K(E;alpha) K(E exp(alpha);beta)`.

The two derivations agree. Within the declared assumptions of a differentiable one-parameter scale group, `alpha_0=Delta ln E`, and action restricted to the capture-cross-section argument, the finite transformation is unique. Those assumptions are the proposed new 6.5.0 closure; they are not retroactively attributed to 6.4.0 or historical 6.3.0.

The equation itself was not selected from the adverse optimum: the hash-frozen pre-fit 6.4.0 response specification already contained `sigma_m(E exp(alpha_0))/sigma_m(E)` as its `finite_dilation_reference`, and used that reference to define the linearization test. STEP 07D adjudicates whether that pre-outcome reference may become an exact production law. The new post-failure decision is its promotion to the owner-gated 6.5.0 predictive closure, not its algebraic invention.

## D. First-order relation to 6.4.0

Taylor expansion about `alpha_0=0` gives:

`sigma(E exp(alpha_0)) = sigma(E) + alpha_0 E sigma'(E) + O(alpha_0^2)`.

After division by positive `sigma(E)`:

`K_m(E;alpha_0) = 1 + alpha_0 E sigma'(E)/sigma(E) + O(alpha_0^2)`.

Therefore:

`S_m(E;alpha_0) = alpha_0 d ln sigma_m(E)/d ln E + O(alpha_0^2)`.

The analytic generator and centered numerical derivative agree at all nine frozen `51Cr`/`37Ar` source lines. The null limit is exact.

## E. Exact observable

Let the baseline measure remain:

`dnu_a = Phi P sigma_m(E) epsilon0_a W_a dE dOmega dt`,

with:

`N0_a = integral_Da dnu_a`.

The exact successor numerator is:

`N_LSC,a(alpha_0) = integral_Da K_m(E;alpha_0) dnu_a`

`= integral_Da Phi P sigma_m(E exp(alpha_0)) epsilon0_a W_a dE dOmega dt`.

Consequently:

`R_pred,a(alpha_0) = N_LSC,a(alpha_0)/N0_a`

`= <K_m(E;alpha_0)>_a`

`= 1 + <S_m(E;alpha_0)>_a`.

This is an exact identity under the already adjudicated normalized E1/E5 measure, not an assumption that reuses the linear formula.

## F. Baseline-measure / cross-section accounting

PASS - no double counting.

The baseline measure already contains exactly one factor `sigma_m(E)`. Multiplication by the ratio `K_m=sigma_m(E exp(alpha_0))/sigma_m(E)` cancels that baseline factor algebraically and replaces it with exactly one shifted cross section in the numerator. The denominator retains the exact conventional `N0_a`.

The transformation does not act on source activity, radioactive decay, line energies or branching fractions, survival probability, detector efficiency, exposure, geometry or nuisance parameters. Phase-space and nuclear-response behavior already encoded inside the frozen `sigma_m` is evaluated at its shifted argument; no separate phase-space factor is added.

## G. Domain

The nonlinear domain is derived before any new fit and does not reuse the linear-response bound.

For cross-section support `[E_min,E_max]` and authoritative baseline energies `{E_i}`:

`max_i ln(E_min/E_i) <= alpha_0 <= min_i ln(E_max/E_i)`.

For the frozen union of `51Cr` and `37Ar` source lines:

- `E_min,payload = 0.24 MeV`;
- `E_max,payload = 30.0 MeV`;
- smallest source line = `0.4264 MeV`;
- largest source line = `0.8138 MeV`;
- exact support domain = `[-0.5747389495096434, 3.607238025076703]`.

This interval follows solely from the frozen payload and authoritative line support. It was not chosen to contain the previous diagnostic optimum. There is no source-derived independent theory bound. Outside the support-derived domain the proposed successor must fail closed.

The unchanged natural cubic spline is finite and positive throughout its closed support, so the exact response ratio is finite and positive for every admitted transformed energy. No new cross-section model is selected.

## H. Parameter count

Exactly one physical LSC parameter remains:

`alpha_0`, dimensionless, signed, universal and equal to the logarithmic shift of the capture-cross-section argument.

The symbol is retained because its physical meaning is unchanged. There is no `beta`, quadratic rescue coefficient, isotope coefficient, experiment coefficient, zone coefficient, tensor, preferred axis or anisotropic amplitude. Nuisance parameters remain outside LSC physics.

## I. Physical interpretation

Candidate X is selected for owner consideration:

`sigma(E) -> sigma(E exp(alpha_0))` while the authoritative source energy `E` itself remains unchanged in the dataset payload.

Candidate Y would replace physical energy throughout the forward model and could alter the source spectrum, survival probability, detector response and geometry-dependent factors. X and Y generally produce different predictions. Y is therefore not an implementation convention and is not admitted under the owner-authorized A2 meaning, which explicitly selected dilation of the capture-cross-section argument only. A future Y proposal would require another owner decision and model identity.

## J. Comparison N1/N2/N3

| Path | Continuity | New assumptions | Physical parameters | Identifiability | Falsifiability and transfer | BEST-2 capability | Version consequence |
|---|---|---|---:|---|---|---|---|
| N1 - exact finite A2 dilation | Preserves the authorized logarithmic capture-argument interpretation and its linear generator | Exact one-parameter group closure after 6.4.0 failure | 1 | one-dimensional after a new protocol freeze | one alpha transfers without refit; source spectra can reject it | schema-capable after authoritative as-built inputs and separate authorization | proposed 6.5.0 |
| N2 - preserve linear failure | Perfect historical preservation; no continuation | none | no new executable parameter | not applicable | no A2 successor prediction | none | retain adverse 6.4.0 only |
| N3 - return to A1/A3 | Changes response family | new scale or detector-response hypothesis and inputs | at least 1 | A1 normalization-degenerate; A3 input-limited | weaker distinction or detector-systematic degeneracy | conditional | new owner-gated minor successor, not 6.4.x |

N1 is preferred because it is the minimal finite completion of the already authorized physical meaning, keeps one parameter, preserves universal sharing and is prospectively falsifiable. Fit quality was not evaluated or used.

## K. Version decision

The successor is **LSC 6.5.0**, not 6.4.1 and not a repaired 6.4.0.

Reason: the exact finite response changes predictions beyond first order and is proposed after seeing the preregistered 6.4.0 development failure. It is a substantive predictive-closure change within the same broad model family, so a minor-version escalation is appropriate. No fundamental model-class change requiring 7.0 was found.

The draft specification is `spec/LSC_6_5_0_EXACT_DILATION_SPECIFICATION.yaml`, SHA-256 `114e8398a45e6a6c8fb4a105e994ca695805b06b9ed8608b86288ea0791ef948`.

## L. Development-data exposure

- BEST inner: `POST_6_4_DEVELOPMENT`, `MODEL_EXPOSED`, `RETROSPECTIVE_NON_BLIND`, `DEVELOPMENT_DATA`.
- BEST outer, GALLEX/GNO and SAGE: not used in the STEP 07C objective and not evaluated in STEP 07D, but remain `MODEL_EXPOSED` and `NON_BLIND_HISTORICAL` under the earlier project exposure audit. They are not upgraded to strict blind holdouts.
- BEST-2: `FUTURE_ONLY`, `FUTURE_BLIND`; no observed data accessed and no prediction generated.

The failed 6.4.0 diagnostic optimum appears only in the adverse record. It was not used to select the finite equation, transformation order, cross-section scenario, additional parameter or nonlinear domain.

## M. Future parameter-determination requirements

`LSC_6_5_0_ALPHA_DETERMINATION_PROTOCOL_DRAFT.json` defines a future, non-executable protocol draft. Its SHA-256 is `2c8e56155e09e8e2a0d93ed8709cca3da036572257b275df1d5847bf3fb13b2b`.

Before any fit, a new pre-fit freeze must bind:

1. explicit owner authorization and its hash;
2. the 6.5.0 scientific specification and exact evaluator;
3. unchanged cross-section and source-line payloads;
4. the support-derived alpha domain;
5. development observation and source hashes;
6. likelihood, covariance, nuisance policy, optimizer and failure rules;
7. independent reference, schemas, golden vectors and environment;
8. no-refit and BEST-2 blindness gates.

The proposed development assignment keeps BEST inner as post-6.4 development data and leaves the previously declared historical holdouts outside the objective. `COV_DIAGONAL` remains assumption-based and sensitivity-only because no collaboration covariance was recovered. The draft authorizes zero executions.

## N. Owner authorization status

`OWNER_AUTHORIZATION_LSC_6_5_0.md` exists with status **PENDING**.

Owner-authorized: NO.

Implementation-authorized: NO.

Parameter-determination-authorized: NO.

Prediction-authorized: NO.

Publication-authorized: NO.

The owner package explicitly acknowledges that 6.5.0 is specified after the adverse 6.4.0 result and cannot be represented as a pre-existing 6.4.0 prediction.

## O. Tests

Dedicated STEP 07D symbolic/scientific suite: **16/16 PASS**.

Full repository suite: **244 passed, 24 subtests passed**.

It covers:

- exact null limit;
- first derivative at zero versus the linear A2 generator;
- finite scale-flow composition;
- support-derived domain and fail-closed boundaries;
- positivity and finiteness;
- exact baseline-measure cancellation/no double counting;
- one physical parameter;
- universal sharing and tensor-off profile;
- Candidate X scope;
- 6.4.0 Git-blob immutability;
- claim boundary;
- pending owner gate;
- absence of a new fit, evaluator or BEST-2 access;
- historical 6.3.0 integrity.

No data-dependent or likelihood alpha scan, new fit, historical holdout evaluation or prediction was executed. Predeclared synthetic alpha cases were evaluated only to test the requested algebraic support, positivity and composition properties.

## P. Historical integrity

- Historical LSC 6.3.0 nested HEAD remains `6ffa07bdde533fcc2a550e36c762b8edd9236c47` with a clean tree.
- Historical archive SHA-256 manifest remains **1882/1882 PASS**.
- LSC 6.3.1 remains `ABANDONED_BEFORE_RELEASE`.
- LSC 6.4.0 remains an immutable adverse development result.
- The cross-section payload and interpolation are unchanged.
- The historical archive and its hash manifest remain subject to the existing integrity gate.

## Q. Git state

- Input HEAD: `e53db798f1f3f448034a6e0af6399e8256189cd5`.
- Branch: `main`.
- Intended commit subject: `Step 07D: preserve LSC 6.4.0 adverse result and adjudicate nonlinear successor`.
- Push: NO.
- Tag: NO.
- Publication: NO.
- Final STEP 07D commit: the commit containing this report; its SHA is reported in the operator handoff because a commit cannot contain its own identity.

## R. Next authorized path

**PATH N1 - exact finite A2 dilation.**

**READY FOR LSC 6.5.0 OWNER AUTHORIZATION: YES**

**READY FOR IMPLEMENTATION AFTER AUTHORIZATION: YES**, conditional on recording the exact authorization and hash-freezing a new implementation/pre-fit contract before any parameter determination.

No implementation, parameter determination, historical evaluation, BEST-2 prediction, release, tag, publication or push is authorized by STEP 07D.

Do not repair the failed linear model. Preserve it. Any exact finite continuation has a new identity, new owner authorization, new preregistration and new version.
