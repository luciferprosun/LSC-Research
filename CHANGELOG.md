# Changelog

## Validation Kernel 0.6.0 — publication metadata finalized 2026-08-19

- Records the owner-authorized inclusion of LINZ HOSS in current Validation
  Kernel creator metadata.
- Adds a durable, bounded contribution record and a separate human-facing
  dedication without changing scientific attribution or historical releases.
- Keeps the Validation Kernel software version at `0.6.0` and the frozen LSC
  scientific model version at `6.5.0`.
- Makes no change to equations, parameters, scientific hashes, validation
  policy, adverse results, or Phase-2 status.

## 6.5.0 — publication package prepared 2026-08-18

- Introduces the exact finite-dilation successor
  `sigma(E exp(alpha_0)) / sigma(E)` with one universal parameter.
- Freezes `alpha_0 = -0.14388795521516184` from the disclosed retrospective,
  non-blind BEST-inner development objective.
- Preserves the LSC 6.4.0 failed linear parameter gate as an immutable adverse
  result.
- Adds independent STEP 08 equation, accounting, domain, third-path,
  parameter-freeze, reproducibility, software, provenance, and release audits.
- Prepares separate GitHub, Zenodo model, and Zenodo Validation Kernel
  artifacts without publishing them.

## 6.4.0 — development parameter gate failed

- Froze the linear A2 response and ran its predeclared parameter determination.
- The gate ended with `DEVELOPMENT_PARAMETER_DETERMINATION_FAILED`; the adverse
  result remains immutable and is not softened by 6.5.0.

## 6.3.1 — abandoned before release

- No executable release was produced. The version is retained only as a
  historical decision boundary.

## 6.3.0 — historical record

- Historical identity, commit, symbolic equations, and Zenodo lineage are
  preserved.
- The complete historical numerical state is not recoverable from the frozen
  evidence and must not be represented as reproducible.
